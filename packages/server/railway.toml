[build]
builder = "nixpacks"
nixpacksConfigPath = "packages/server/nixpacks.toml"

[deploy]
startCommand = "bun src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
